<%= @order_user.email %> just made a new order!
==========================================================

Here is what he bought:

<!-- We do a loop in order to display in the mail the title and the image -->
<% @cart_items_ordered.each do |item| %>

<%= item.title %>

You can access the image at <%= item.image_url%>

<% end %>

A new order has just been completed!