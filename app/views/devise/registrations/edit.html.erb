<!DOCTYPE html>
<html>
<head>
  <title>edit profil</title>
  <style type="text/css">
body{

  background-image: url(https://cmkt-image-prd.global.ssl.fastly.net/0.1.0/ps/1794636/580/386/m1/fpnw/wm0/elegant-cat-vet-store-shop-logo-template-01-.png?1477280363&s=cb62b04325cfcfd207736a843daab683);
}
.content{

  background-image: linear-gradient(to right, white, WhiteSmoke);
  margin-left: 400px;
  margin-right: 400px;
  height: 730px;
}   
.zone{

  width:300px;
    height:40px;
    border-left: none;
    border-right: none;
    border-top: none;
     background-color: transparent;
     font-size: 20px;
     color: pink;
     font-family: roboto;

}
.btn{
  width: 100px;
  background-color: Maroon;
  border: none;
  font-size:20px;
  font-family: sans-serif;
  font-weight: bold;

  }
  </style>


</head>
<body>
  <div class="content"><center>
<h2>Edit <%= resource_name.to_s.humanize %></h2>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email",:class=>'zone' %>
  </div>
   <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name, autofocus: true,:class=>'zone'%>
  </div>

  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name, autofocus: true,:class=>'zone'%>
  </div>
  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
    <%= f.password_field :password, autocomplete: "new-password",:class=>'zone' %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> characters minimum</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password",:class=>'zone' %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password",:class=>'zone' %>
  </div><br>

  <div class="actions">
    <%= f.submit "Update",:class=>'btn' %>
  </div>
<% end %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>
</div></center>
</body>
</html>