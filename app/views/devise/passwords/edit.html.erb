
<!DOCTYPE html>
<html>
<head>
  <title>Sign in</title>
  <style type="text/css">
 
 body{

      background-image: url(https://cdn.pixabay.com/photo/2018/03/27/17/26/cat-3266675_960_720.jpg);
      background-size:cover;
      background-repeat:no-repeat;

 }
.content{
  padding-top: 30px;
  margin-left:150px;
  margin-right: 800px;


}
 .field{
  position: center;
   height: 100px;
  width: 500px;   
 }
 .zone{
    width:1300px;
    height:100px;
    border-left: none;
    border-right: none;
    border-top: none;
     background-color: transparent;
     font-size: 50px;
     color: white;
     font-family: roboto;

 }
 h2{
 margin-left:400px;
 color: white;
 font-size: 70px;
 font-family:roboto; 
 }
 .checkbox{
  font-size: 20px;
  color: white;
 }
 .bouton{
  width:150px;
  height:50px;
  background-color:#FF1493;
  border: none;
  font-size: 30px;
 }
  </style>
</head>
<body>
<div class="content">
<center><h2 >Change your password</h2></center>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field">
      <%= f.hidden_field :reset_password_token ,:class=>'zone',:value=>"Reset"%>
  </div>
 
  <div class="field">
    <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em><br />
    <% end %>
    <%= f.password_field :password,autofocus: true, autocomplete: "new-password",:class=>'zone',:value=>"New Password" %>
  </div>
<div class="field">
<%= f.password_field :password_confirmation,autofocus: true, autocomplete: "new-password",:class=>'zone',:value=>"Password confirmation " %>
  </div>
  <div class="actions">
    <%= f.submit "Change password",:class=>"bouton"%>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>

</body>
</html>

